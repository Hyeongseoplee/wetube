mixin video(info)
    div
        h4
            a(href="/videos/" + info.id)=info.title
            ul 
                li #{info.views} views
                li updated #{info.createdAt}
                li #{info.comments} comments
                li #{info.rating} / 5
                li=info.like
                li=info.dislike